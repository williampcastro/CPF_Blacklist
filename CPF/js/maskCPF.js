$(document).ready(function(){ //when the page is ready, this function will be executed

  $('#cpf').mask('000.000.000-00');
  $('#CPF_num').mask('000.000.000-00'); //apply the mask in the input that have this id(CPF_num,cpf)
  //this mask is in jquery.mask.min

});
